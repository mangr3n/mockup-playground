{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "package": "zip -r ov_components.zip build/*",
    "clean": "rm ov_components.zip && rm -R build",
    "full": "npm run build && npm run package"
  },
  "devDependencies": {
    "@snowpack/plugin-postcss": "^1.1.0",
    "@snowpack/plugin-svelte": "^3.5.2",
    "@snowpack/plugin-webpack": "^2.3.1",
    "autoprefixer": "^10.2.5",
    "postcss": "^8.2.8",
    "postcss-apply": "^0.12.0",
    "postcss-cli": "^8.3.1",
    "snowpack": "^3.0.1",
    "tailwindcss": "^2.0.3"
  },
  "dependencies": {
    "svelte": "^3.35.0",
    "yarn": "^1.22.10"
  }
}
